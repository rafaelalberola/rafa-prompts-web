# raw course material (PDFs live in Gumroad, not the public site)
course/pdfs/
course_content/

# build helpers (not needed at runtime on Pages)
scripts/*
# excepción: scripts ejecutados por GH Actions (rotación de Meta Ads)
!scripts/meta_ads
scripts/meta_ads/*
!scripts/meta_ads/*.py

# meta ads creative artifacts (HTML/PNG/script for paid campaigns, not for the public site)
ads/

# local env / secrets
.env
.env.local
.dev.vars

# OS / editor
.DS_Store
*.log
.vscode/
.idea/

# node / cloudflare
node_modules/
.wrangler/
